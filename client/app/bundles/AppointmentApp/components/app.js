import React from 'react';
import ControlPanel from '../containers/controlPanel'

const app = () => {
    return (
        <div className="container">
            <ControlPanel/>
        </div>
    );
};

export default app;